//colour-shifting hypnotising zooming rings

voronoi(6,2,3)
  .color(() => Math.sin(time)*0.5 + 1, () => Math.sin(time*1.3)*0.5 + 1, 0)
  .brightness(0.15)
  .modulateRepeat(osc(10), 3.0, 3.0, 0.5, 0.5)
  .modulateRepeatY(osc(10), 5.0, ({time}) => Math.sin(time) * 5)
  .kaleid(50)
  .out(o0)
